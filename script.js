
// create an account object

const account = {
   
    name: "Cactus",
  };
  
 
  
 // create a function called menu()

  function menu() {
  
    const choice = parseFloat(
      prompt(
        "Please select from the menu:\n1) Add income\n2) Add expense\n3)List all expenses\n4) See total balance"
      )
    );
  
    console.log("Choice: " + choice);
    const amount = parseFloat(prompt("How much was your incom?")("How much was your expence?"));

    

   

  

   
    
  }
  
  menu();
 